1. Install oh-my-zsh (https://ohmyz.sh/).
2. Copy or symlink `af-magic-mod.zsh-theme` to `$HOME/.oh-my-zsh/custom/themes/af-magic-mod.zsh-theme`. 
3. Copy or symlink  `zshrc` to `$HOME/.zshrc`.
4. Install plugins as indicated in `zshrc` comments near the plugins section.
